#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define str string
void solve(){
    str s ; cin >> s;
    ll cnt[5];
    for (const ll &i : s){
        if (i=='I') cnt[0]++;
        if (i=='V') cnt[1]++;
        if ()
    }
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}